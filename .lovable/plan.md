## Что сломалось

Я убрал растяжку на всю высоту экрана — поэтому весь контент схлопнулся вверх. Надо вернуть полную высоту мобильного Hero и переложить так, чтобы **верхняя часть (дата + заголовок) осталась там, где была**, а **нижний блок (KPI + кнопка) поднялся ровно на половину** прежнего разрыва.

## Правки в `src/components/pravda/PravdaLanding.tsx`, Hero (~744–805)

1. Вернуть мобильную высоту:
   - `Container`: вернуть `min-h-[calc(100svh-68px)]` (для md — `md:min-h-0`).
   - Внутренний колоночный `<div>`: вернуть `min-h-[calc(100svh-124px)] flex flex-col`, но **без `justify-between`**.
2. Внутри колонки разложить через явные распорки (только на мобиле, на md — `md:hidden`):
   - пилюля даты
   - `<div className="flex-1 md:hidden" />` ← верхний воздух (как раньше)
   - заголовок + дескриптор
   - `<div className="basis-0 grow-[0.5] md:hidden" />` ← нижний воздух = половина верхнего
   - блок KPI + кнопка
3. Десктоп (`md:`) не трогаем: остаётся grid с прежними отступами.
4. Убрать добавленные мной мобильные `mt-8` / `mt-10` между секциями — расстояния задают распорки.

Итог: на мобиле дата сверху, заголовок примерно по центру (как раньше), KPI+кнопка поднимаются вверх вдвое ближе к заголовку.