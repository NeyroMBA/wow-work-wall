План исправления:

1. Проверить сами файлы изображений, которые ломаются на мобильном:
   - `dashboard-screenshot.png` — именно он показывается в мобильной версии блока «Дизайн дашбордов»;
   - `dashboard-illustration.png` — desktop/tablet;
   - `metric-map.png` — карта метрик.

2. Убрать текущую зависимость мобильной картинки от asset-JSON URL вида `/__l5e/assets-v1/...`, потому что файл по серверу отдаётся `200 OK`, но на реальном мобильном браузере картинка всё равно отображается как broken image. Значит проблема не в кеше и не в вёрстке контейнера, а в способе доставки/декодирования именно этого asset URL на мобильном.

3. Переподключить проблемные изображения как обычные статические файлы проекта через Vite-импорт:
   - скачать текущие рабочие PNG из asset URL;
   - положить их в `src/assets/pravda/`;
   - импортировать напрямую в `PravdaLanding.tsx`;
   - заменить `dashboardMobileImg.url`, `dashboardImg.url`, при необходимости `metricMapImg.url` на прямые импортированные пути.

4. Для мобильной версии убрать потенциально конфликтную связку `picture + lazy + asset-json`:
   - оставить `<picture>` для desktop/mobile, но с прямыми статическими URL;
   - для изображения дашборда в программе поставить `loading="eager"` и `decoding="async"`, потому что оно находится достаточно высоко и сейчас именно там виден broken image;
   - для повторного изображения в блоке «Вы сможете после тренинга» можно оставить `loading="lazy"`, но уже с тем же прямым статическим URL.

5. Проверить на мобильном viewport:
   - картинка дашборда видна, не broken icon;
   - квадратная мобильная версия сохранена;
   - desktop/tablet остаются как сейчас;
   - вёрстку и порядок блоков не трогать.